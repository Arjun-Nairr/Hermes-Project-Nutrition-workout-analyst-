DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
SITE_PASSWORD=choose-a-password
# Long random string — this is what Hermes sends as its Bearer token to /api/mcp.
MCP_SECRET=
# Free key from https://fdc.nal.usda.gov/api-key-signup.html
USDA_API_KEY=
